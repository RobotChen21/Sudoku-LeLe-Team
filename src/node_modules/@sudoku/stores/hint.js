import { writable } from 'svelte/store';

function createHint() {
	const hint = writable(null);

	return {
		subscribe: hint.subscribe,

		set: (nextHint) => hint.set(nextHint),

		clear: () => hint.set(null),
	};
}

export const hint = createHint();
